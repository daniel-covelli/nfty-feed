import React from 'react';
import { Text } from '@chakra-ui/react';

interface ProfileProps {}

export const Profile: React.FC<ProfileProps> = ({}) => {
  return <Text>There is nothing on this page yet</Text>;
};
