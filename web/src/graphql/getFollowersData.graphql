query GetFollowersData($userId: Float!) {
  getFollowersData(userId: $userId) {
    profile {
      id
      username
      profileImageId
      first
      last
    }
    following
    me
    userId
  }
}
