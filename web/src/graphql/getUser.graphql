query GetUser($path: String!) {
  getUser(path: $path) {
    me
    user {
      id
      email
      profile {
        id
        username
        phone
        first
        last
        bio
        profileImageId
      }
    }
  }
}
