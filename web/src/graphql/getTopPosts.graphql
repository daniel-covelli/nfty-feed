query GetTopPosts($page: Float!) {
  getTopPosts(page: $page) {
    id
    owner {
      id
      username
      profileImageId
      first
      last
    }
    media
    title
    artist
    link
    type
    visibility
    removed
    numberOfLikes
    createdAt
  }
}
