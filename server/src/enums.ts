export enum SubStatus {
  ACTIVE = 1,
  INACTIVE = 0
}

export enum VisStatus {
  VISIBLE = 1,
  HIDDEN = 0
}

export enum GlobalStatus {
  VISIBLE = 1,
  REMOVED = 0
}

export enum AdminStatus {
  ADMIN = 1,
  NORMY = 0
}
