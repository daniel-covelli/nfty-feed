{
  "private": true,
  "scripts": {
    "build:server": "yarn --cwd ./server build ",
    "build:web": "yarn --cwd ./web build "
  },
  "workspaces": {
    "packages": [
      "server/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
