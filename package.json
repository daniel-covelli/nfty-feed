{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope server"
  },
  "workspaces": [
    "server/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "nfty-feed"
}
