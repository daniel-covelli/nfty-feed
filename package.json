{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope server"
  },
  "workspaces": {
    "packages": [
      "server/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
