{
  "private": true,
  "scripts": {
    "build:server": "yarn --cwd ./server build",
    "build:web": "yarn --cwd ./web build",
    "deploy:web": "sh ./deploy_web_netlify.sh"
  },
  "devDependencies": {
    "lerna": "^8.0.0",
    "netlify-cli": "^17.33.3"
  }
}
