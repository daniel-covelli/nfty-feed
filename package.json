{
  "private": true,
  "scripts": {
    "build:server": "yarn --cwd ./server/ build "
  },
  "workspaces": {
    "packages": [
      "server/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
